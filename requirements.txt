# SmarTraid runtime dependencies.
#
# This file was previously saved as UTF-16, which made `pip install -r
# requirements.txt` fail before installing anything. Keep it UTF-8.

# --- core data ---
numpy==1.26.4
pandas==2.0.3
polars==1.8.1

# --- market data ---
yfinance==1.2.1
requests==2.32.3
pytz==2024.2
lxml==5.3.0          # pandas.read_html parser for the Wikipedia S&P 500 table
html5lib==1.1        # read_html fallback parser

# --- monitor station (server/web) ---
fastapi==0.141.1
uvicorn[standard]==0.52.1
sse-starlette==3.4.6

# --- RL trading agent (server/rl) ---
# This resolves a CUDA build on some platforms, which is a large download.
# For a CPU-only install (all that's needed to run the tests or train on a
# laptop):
#   pip install torch==2.13.0 --index-url https://download.pytorch.org/whl/cpu
torch==2.13.0

# --- optional legacy ML subsystem (prediction.py / training.py) ---
# Only needed to run the regime/price models, which also require MongoDB.
scikit-learn==1.4.2
joblib==1.4.2        # model serialisation, replaces raw pickle
hmmlearn==0.3.2
pymongo==4.5.0

# --- tests ---
pytest==9.0.3
